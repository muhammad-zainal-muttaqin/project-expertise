# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
.venv/
.venv-audit/
.pytest_cache/
venv/
env/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Dataset (terlalu besar untuk git)
datasets/
SawitMVC-YOLO/
SawitMVC-Depth/

# Bobot model dan log training lokal (backup di luar Git)
*.pt
*.pth
models/*.txt
models/**/*.txt

# Artefak analisis/unggahan lokal
Manus/

# Hasil training besar
runs/
wandb/

# Temp
temp/
*.tmp

# Environment / secrets
.env
.env.*

# Hasil training Fase 6 (bobot besar; ikut backup HF bucket, ringkasan angka ada di results/)
runs_fase6/

# Hasil training new763 & combined1716 (2026-08-22, bobot besar GB-an; ikut
# backup HF bucket manual, ringkasan angka + riwayat epoch ada di results/)
runs_new763/
runs_combined1716/
runs_new763_rgbd4/
runs/detect/runs_new763/
runs/detect/runs_combined1716/
runs/detect/runs_combined1716_quicktest/

# Checkpoint autosave Jupyter
.ipynb_checkpoints/

# Cache label Ultralytics — binary, regenerable otomatis saat run berikutnya
*.cache

# runtime pipeline matriks mono-depth (2026-08-15)
.matriks.lock
.pantau.lock
.boot6_dilaporkan
.build352.log
# Log mentah ~12 MB, penuh progress bar. Versi ringkasnya ada di
# logs_ringkas/ dan ITU yang ikut ter-commit (lihat CLAUDE.md).
logs/

# Cache sub-proyek pipeline-pertandan (2026-08-17) — semuanya REGENERABLE,
# resep di pipeline-pertandan/README.md. Yang WAJIB tetap ikut git adalah dump
# prediksi `pred_skorpenuh*.npz` (~9 MB): itu yang membuat seluruh angka bisa
# dihitung ulang tanpa GPU dan tanpa bobot (lihat ATURAN #1 di /workspace/CLAUDE.md).
# CATATAN: .gitignore TIDAK mendukung komentar di akhir baris — komentarnya ikut
# jadi bagian pola dan aturannya diam-diam tidak berlaku. Sudah terjadi sekali di
# sini: cache 981 MB nyaris ikut ter-commit.
# potongan_reid*.npz  = 981 MB, potongan citra 128x128 untuk melatih re-ID
pipeline-pertandan/results/potongan_reid*.npz
# deskriptor_crop*.npz = 10 MB, histogram HSV per potongan
pipeline-pertandan/results/deskriptor_crop*.npz
# reid_embedding*.npz  = 39 MB, turunan bobot re-ID
pipeline-pertandan/results/reid_embedding*.npz
# Crop resolusi tinggi DAMIMAS: cache uint8 multi-GB, dapat dibuat ulang dari
# citra/label memakai scripts/bangun_crop_damimas.py. Metadata dan manifest kecil
# tetap dilacak agar urutan crop serta provenance dapat diaudit.
pipeline-pertandan/results/crop_damimas_*.npy
pipeline-pertandan/results/crop_damimas_*.partial.npy
# Crop proposal nyata untuk classifier B1--B4+background. Metadata/manifest
# kecil tetap dilacak; tensor uint8 multi-GB dapat diregenerasi dari dump NPZ.
pipeline-pertandan/results/crop_proposal_*.npy
pipeline-pertandan/results/crop_proposal_*.partial.npy
# Graf deteksi/linker berisi fitur yang dapat diregenerasi dari dump prediksi.
pipeline-pertandan/results/cache_linker_damimas_*.joblib
# Probabilitas classifier pada proposal fusion; regenerable dari checkpoint.
results/cache_relabel_damimas_*.joblib

# Keluaran render Markdown -> PDF (.agents/skills/markdown-ke-pdf). Berkas .tex
# adalah perantara dan .pdf dapat dibangun ulang kapan saja dari sumber .md-nya,
# sehingga tidak dilacak agar riwayat tidak menyimpan biner yang berubah setiap
# kali dokumen sumbernya disunting.
metrics/*.tex
metrics/*.pdf
